from setuptools import setup, find_packages

setup(
    name="RABIES_ISRAEL_PROJECT",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "jupyter-book",
        "pytest",
        "requests",
        "beautifulsoup4",
        "numpy",
        "pandas",
        "matplotlib",
        "plotly",
        "requests",
        "htmlmin"
    ],
)
